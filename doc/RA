# 录画系统 · 需求规格

## 1. 系统定位

* 本系统为无 UI 的后台服务（server process）。
* 通过外部 client 调用进行控制与配置。

---

## 2. 输入源需求

### 2.1 视频输入源

* 支持多种视频源类型：

  * 本地物理摄像头
  * 网络视频流
  * 本地视频文件
* 视频输入源在系统中视为同等实体。
* 支持至少两路同时有效的视频输入源。

### 2.2 音频输入源

* 支持多种音频源类型：

  * 本地麦克风
  * 网络音频流
  * 本地音频文件
* 音频输入源可独立存在。

---

## 3. 动态源管理

* 支持运行过程中动态添加视频输入源。
* 支持运行过程中动态移除视频输入源。
* 单一路输入源的添加、移除或异常：

  * 不应中断其他已存在输入源的采集。

---

## 4. 字幕需求

* 支持基于音频生成字幕文本。
* 字幕条目需包含：

  * 开始时间
  * 结束时间
  * 文本内容
* 字幕生成延迟或失败：

  * 不应影响视频或音频录画。
* 字幕生成与音频输入源类型无关。

---

## 5. 输出需求

* 系统需将录画结果输出为文件。
* 输出文件内容可包含：

  * 一路或多路视频
  * 音频（若存在）
  * 字幕（若生成）
* 输出文件格式：

  * 由 client 端指定；
  * 若未指定，默认使用 MP4 格式。
* 输入源动态变化过程中：

  * 输出文件应保持格式与数据完整性。

---

## 6. 约束与非目标

* 系统不提供 UI。
* 系统不负责播放、编辑或网络分发。
* 系统不提供输入源自动发现能力。

---

## 7. 需求阶段刻意不定义内容

* 内部模块或架构划分
* 状态管理方式
* pipeline / 线程 / task 组织方式
* 具体技术选型
